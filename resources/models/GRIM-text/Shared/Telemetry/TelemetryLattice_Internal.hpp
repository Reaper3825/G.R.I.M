#pragma once
// Pattern B: TelemetryLattice class definition is now in TelemetryLattice_GPU.hpp.
// This redirect avoids breaking .cu files that previously included this header.
#include "TelemetryLattice_GPU.hpp"
