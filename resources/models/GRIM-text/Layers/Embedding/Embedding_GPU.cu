//======================================================//
//  Embedding_GPU.cu
//
//  EmbeddingLayer was deleted. Startup embedding parameter allocation now
//  lives in `training/Phases/Startup/Model/ParameterGroupRegistration.cu`
//  as registry-owned `EmbeddingParameterTensors`.
//
//  This translation unit remains intentionally empty so existing build lists
//  do not fail while the deletion propagates through generated project files.
//======================================================//
