# Local MediaPipe assets

Place an audited local MediaPipe Gesture Recognizer task model here as:

`gesture_recognizer.task`

GRIM never downloads this asset. The model binary is intentionally not included
in the repository; record its source, license, version, and checksum in release
provenance before distribution.
