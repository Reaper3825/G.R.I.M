# GRIM Python Dependencies - Fixed for XTTS v2
# Coqui TTS - XTTS v2 for high-quality text-to-speech
TTS>=0.22.0

# Deep learning framework (required by XTTS)
# Note: PyTorch 2.7+ has breaking changes, pinning to stable version
torch>=2.0.0,<2.7.0
torchaudio>=2.0.0,<2.7.0

# Transformers and tokenizers (required by XTTS v2 GPT model)
transformers==4.33.0
tokenizers>=0.13.0
accelerate>=0.20.0

# Audio processing
numpy==1.22.0
scipy>=1.7.0
soundfile>=0.12.0
librosa>=0.10.0

# Protocol buffers (compatible version)
protobuf==3.20.3

# Utilities
pydub>=0.25.1
inflect>=5.6.0
unidecode>=1.3.0

# Optional: GPU acceleration (CUDA)
# If you have NVIDIA GPU, install: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
