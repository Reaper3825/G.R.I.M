#pragma once
#include "ui_panel.hpp"
#include "ui_graph.hpp"
#include "ui_button.hpp"
#include <memory>
#include <vector>

// Demo panel to showcase all UIGraph capabilities
class UIGraphDemoPanel : public UIPanel {
public:
    UIGraphDemoPanel();
    
    void update(const InputState& input, float dt) override;
    void drawOverlay(OverlayRenderer& renderer) override;
    
private:
    // Graphs
    std::shared_ptr<UIGraph> lineGraph;
    std::shared_ptr<UIGraph> barGraph;
    std::shared_ptr<UIGraph> pieChart;
    std::shared_ptr<UIGraph> multiLineGraph;
    std::shared_ptr<UIGraph> realtimeGraph;
    
    // Controls
    std::shared_ptr<UIButton> btnSwitchType;
    std::shared_ptr<UIButton> btnAddData;
    std::shared_ptr<UIButton> btnClearData;
    std::shared_ptr<UIButton> btnToggleAnimation;
    std::shared_ptr<UIButton> btnRandomize;
    
    // State
    int currentGraphType = 0;
    float updateTimer = 0.0f;
    int dataPointCounter = 0;
    std::string statusMessage;
    
    // Helper methods
    void initGraphs();
    void initControls();
    void switchGraphType();
    void addRandomData();
    void clearAllData();
    void toggleAnimation();
    void randomizeData();
    void updateRealtimeGraph(float dt);
    
    // Demo data generators
    std::vector<DataPoint> generateTrendData(int points, float trend = -0.05f);
    std::vector<DataPoint> generateRandomData(int points, float min = 0.0f, float max = 100.0f);
    std::vector<DataPoint> generateCategoricalData();
};
